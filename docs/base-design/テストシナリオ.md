# テストシナリオ

> 工程2 UI（基本設計）の正式成果物。`docs/base-design/テストシナリオ.md` に直接生成する（**UI issue単位・案件横断で累積**）。
> PT-Plan・PT工程はこのファイルの受け入れ条件・観点を入力としてテストコード・テストdocsを生成する。

> ⚠️ **スコープは frontend（front-intra-001〜007）のみ**。`bs-001`〜`bs-009` は bs スタック未着手のため、APIテスト観点は「フロントエンドが依存する参考シナリオ」として記載するに留め、bs側の詳細なテスト実装観点（DBセットアップ・排他制御等の実装依存項目）は本ファイルでは確定しない。bs側の正式なテストシナリオは bs の SS/PT 工程で別途作成する。

## メタ情報
- 対象要件ID: R001, R002, R003, R004, R005, R006, R007（全件）
- 案件キー: （本移行では未設定。既存システムの設計書移行のため案件フロー外）
- UI issue-id: （なし。旧設計書 `_src`（reacter-react-tsx2doc-試作）からの移行）
- 作成日: 2026/08/20

## 受け入れ条件カバレッジ

> `docs/requirements/要件定義_{要件ID}.md`（R001〜R007、全21件）の受け入れ条件と、それをカバーするテストケースの対応を記載する。1件の漏れもないことを確認済み。

| # | 要件ID | 受け入れ条件（要件定義_{要件ID}.md から転記） | カバーするテストケース番号 |
|---|---|--------------------------------------|----------------------|
| 1 | R001 | お知らせ・お問い合わせ先が静的コンテンツとして表示される | CMP-表示-1, E2E-001 |
| 2 | R001 | 「メニュー画面へ」ボタン押下でメニュー画面（未ログイン時はログイン画面）へ遷移する | CMP-操作-1, E2E-001, E2E-002 |
| 3 | R001 | 「メールはこちら」リンク押下で別タブにメール作成画面が起動し、この画面は表示されたままとなる | CMP-操作-2 |
| 4 | R002 | 正しいユーザーID・パスワードでログインでき、ユーザー情報が保持される | CMP-操作-3, API-正常-1（参考）, E2E-001 |
| 5 | R002 | 誤った認証情報の場合、エラーメッセージが表示され画面に留まる | CMP-操作-4, API-異常-1（参考）, E2E-004 |
| 6 | R002 | 未ログインで保護画面にアクセスした場合、ログイン後に元の画面へ遷移する | CMP-操作-5, E2E-002 |
| 7 | R003 | 一般ユーザー権限では「管理者用」ボタンが表示されない | CMP-表示-2 |
| 8 | R003 | 管理者権限では「管理者用」ボタンが表示され、押下で管理者用データ取り込み画面へ遷移する | CMP-表示-3, CMP-操作-6, E2E-003 |
| 9 | R003 | 未ログインでこの画面のURLに直接アクセスした場合、ログイン画面へ遷移する | E2E-002 |
| 10 | R004 | 正しい工番コード・年度で検索すると収支一覧・合計表が表示される | CMP-操作-7, API-正常-2（参考）, E2E-005 |
| 11 | R004 | 並び替え操作で通信を発生させずに一覧の表示順が変わる | CMP-操作-8, CMP-状態-1 |
| 12 | R004 | 工番検索ダイアログから工番を選択すると、検索条件に反映されそのまま収支データが検索される | CMP-操作-9, API-正常-3（参考） |
| 13 | R005 | 検索条件に一致するコスト利用率データが一覧表示される | CMP-操作-10, API-正常-4（参考） |
| 14 | R005 | 一覧行をダブルクリックすると工番コード・年度を引き渡して工番別収支データ参照画面へ遷移する（工番コードが存在する行のみ） | CMP-操作-11, E2E-006 |
| 15 | R005 | セルの値に応じて背景色が変わる | CMP-表示-4 |
| 16 | R006 | フォルダを選択して送信すると、配下のCSVファイルが取り込まれ登録件数が通知される | CMP-操作-12, API-正常-5（参考）, E2E-007 |
| 17 | R006 | ファイル未選択・空ファイル混在・CSV形式なしの場合はエラーとなり送信されない | CMP-検証-1, CMP-検証-2, CMP-検証-3 |
| 18 | R006 | CSV変換マクロファイルのダウンロードができる | CMP-操作-13 |
| 19 | R007 | 管理者権限で各フォームからCSVを取り込むと、登録件数がトースト通知される | CMP-操作-14, API-正常-6（参考） |
| 20 | R007 | 一般ユーザー権限でこの画面のURLに直接アクセスした場合、メニュー画面へリダイレクトされる | E2E-003 |
| 21 | R007 | 未ログインの場合、ログイン画面へ遷移する | E2E-002 |

**総数: 21件 ／ カバー: 21件（カバレッジ 100%）**

## ユニットテスト観点

> frontend の共通ロジック（Component/Hookに属さない純粋関数）を対象とする。Component/Hook・Zodスキーマのテストは「コンポーネントテスト観点」節で扱う。

| # | テスト対象 | 条件 | 期待結果 |
|---|----------|------|--------|
| UT-1 | `utils/commonUtil.addCommasToNumber` | 正の整数（例: 1234567）を渡す | 3桁ごとにカンマが挿入された文字列（例: "1,234,567"）を返す |
| UT-2 | `utils/commonUtil.convertYojitsuFlgToValue` | `yojitsuFlg` に計画フラグ／実算フラグ／利用率フラグ／未定義を渡す | それぞれ対応する表示文字列（"計画"/"実算"/"利用率"等）を返す。未定義時は空文字を返す |
| UT-3 | `utils/commonUtil.convertHiddenRow` | 実算行（`yojitsuFlg`が実算）で値を渡す | 計画行と重複する項目（部署/社員番号/氏名）を空文字に変換して返す（非表示行判定） |
| UT-4 | `utils/commonUtil.createNendoList` | 呼び出し時の現在日付を基準とする | `SelectOptionType[]` 形式で年度選択肢のリストを返す（最終要素が最新年度） |
| UT-5 | `utils/commonUtil.createYearMonthList` | 呼び出し時の現在日付を基準とする | `SelectOptionType[]` 形式で年月選択肢のリストを返す |
| UT-6 | `utils/commonUtil.getBushoCdList` | 部署コード一覧取得API（`bs-001`参考）が正常応答する | `SelectOptionType[]`（`label`・`value`とも部署コード文字列）を返す |
| UT-7 | `lib/messageInitializer.getMessage` | メッセージコード＋置換パラメータ（例: 項目名）を渡す | パラメータが埋め込まれたメッセージ文言を返す |
| UT-8 | `hooks/useLoading.runWithLoading` | 渡した非同期関数の実行中 | 実行前に `isLoading` を `true` に、実行完了後（例外時も含め`finally`で）`false` に戻す |
| UT-9 | `hooks/useErrorHandling.axiosErrorHandling` | Axiosエラーでない一般の例外を渡す | システムエラーメッセージのトーストを表示する（`MESSAGE_CODE.ERROR.COMMON.SYSTEM_ERROR`） |
| UT-10 | `hooks/useErrorHandling.axiosErrorHandling` | レスポンスステータス401（`errorCode`がサーバエラー系）を渡す | エラートーストを表示し、3秒後に自動ログアウト（`logout()`呼び出し）する |
| UT-11 | `hooks/useErrorHandling.axiosErrorHandling` | `errorCode`がバリデーションエラー・`detail`が存在するレスポンスを渡す | `detail[].ValidationMessage` を改行結合したメッセージをトースト表示する |

## APIテスト観点

> ⚠️ 以下は `bs-001`〜`bs-009`（bs スタック未着手）を対象とした**参考シナリオ**である。フロントエンドの `axios` 呼び出し・エラーハンドリング実装（`useErrorHandling`・各画面の`try/catch`）が前提とする挙動を明確化する目的で記載し、bs側の正式なテスト実装はbsのSS/PT工程で確定する。

### テスト前提条件（DB セットアップ）

> bs 未着手のため DB セットアップは未確定。以下は旧設計書（`old_docs/03.Web_API_IF定義書/`）から読み取れる前提を参考として記載する（bs側SS工程で正式化）。

```sql
-- 前提データ例（参考。bs側SS工程で正式なDDL・テーブル定義に基づき再作成すること）
-- ユーザー認証用（bs-002 LOGIN の前提。テーブル名・カラム名は仮称）
-- INSERT INTO T_USER (usr_id, password, role, version)
--   VALUES ('U0001', 'hashed_pw', 'user', 0);
-- INSERT INTO T_USER (usr_id, password, role, version)
--   VALUES ('A0001', 'hashed_pw', 'admin', 0);

-- 工番マスタ（bs-004 SEARCH_KOBAN / bs-005 SEARCH_KOBANBETSU_SHUSHI の前提）
-- INSERT INTO T_KOBAN (koban_cd_oya, koban_cd_ko, koban_nm, priority)
--   VALUES ('100001', '01', 'サンプル工番', '1');
```

### 正常系（参考）

| # | 操作 | 入力 | 期待結果 |
|---|-----|------|--------|
| API-正常-1 | `POST /login`（bs-002 参考） | 正しいユーザーID・パスワード（FormData） | 200 OK。`accountNonExpired`・`accountNonLocked`・`authorities`・`credentialsNonExpired`・`enabled`・`username` を含むレスポンスを返す |
| API-正常-2 | `GET /kobanbetsu-shushi`（bs-005 参考） | 存在する工番コード（親番・子番）・年度 | 200 OK。`kobanInfo`・`recordList`（社員ごとの計画/実算）・`sokuhoFlgMap`・`keikakuTotal`・`jissanTotal`・`usageRate`・`registerDate` を返す |
| API-正常-3 | `GET /search-koban`（bs-004 参考） | 工番コード親番の前方一致検索 | 200 OK。条件に一致する工番情報（`kobanCdOya`/`kobanCdKo`/`kobanNm`/`priority`）の配列を返す |
| API-正常-4 | `GET /cost-riyoritsu`（bs-003 参考） | 存在する工番・年度・部署コード | 200 OK。`registerDate`・`usageRate`（`ShushiKanriType[]`）を返す |
| API-正常-5 | `PUT /upload-prokandata`（bs-006 参考） | 正常なCSVファイル（複数） | 200 OK。`insertCount`・`deleteCount` を返す |
| API-正常-6 | `PUT /upload-shakatsudata`／`upload-tankadata`／`upload-sofukaridata`（bs-007〜009 参考） | 正常なCSVファイル（＋年月／年度） | 200 OK。`insertCount`（`upload-shakatsudata`・`upload-sofukaridata`は`deleteCount`も）を返す |

### 異常系（参考）

| # | 操作 | 入力 | 期待結果 |
|---|-----|------|--------|
| API-異常-1 | `POST /login`（bs-002 参考） | 誤ったユーザーID・パスワード | 401（想定）。フロントエンドは `useErrorHandling` で判定し、`errorCode`がサーバエラー系であればエラートーストを表示（自動ログアウトは元々未ログインのため発火しない） |
| API-異常-2 | `GET /kobanbetsu-shushi`（bs-005 参考） | 桁数不正な工番コード（バリデーションエラー） | 400（想定）。`errorCode`=`error.unexpect.method-argument-not-valid` 相当、`detail[].Field`/`ValidationMessage` を含むレスポンス。フロントエンドは `detail[].ValidationMessage` を結合してトースト表示 |
| API-異常-3 | `PUT /upload-prokandata`（bs-006 参考） | サーバ側でCSV内容不整合を検知 | 409（想定。排他制御・データ不整合系）。フロントエンドはエラートーストを表示し選択状態を保持（`resetField`を呼ばない） |

### 境界値（参考）

| # | 項目 | 境界値 | 期待結果 |
|---|-----|-------|--------|
| BV-1 | 工番コード親番（`kobanCdOya`） | 6桁固定（`kobanbetsuShushiSchema`） | 5桁以下・7桁以上はフロントエンドのZodバリデーションでエラー（サーバに送信されない） |
| BV-2 | 工番コード子番（`kobanCdKo`） | 2桁固定 | 1桁・3桁以上はフロントエンドのZodバリデーションでエラー |
| BV-3 | 部署コード（`bushoCd`） | 4桁固定（`costRiyoritsuSchema`。未入力時は全件検索扱いで許容） | 4桁以外はフロントエンドのZodバリデーションでエラー |
| BV-4 | 工番名（`kobanNm`） | 最大15桁（`costRiyoritsuSchema`/`kobanSearchDialogSchema`） | 16桁以上はフロントエンドのZodバリデーションでエラー |

### 認証・認可テスト（参考）

| # | 操作 | 認証状態 | 期待結果 |
|---|-----|---------|--------|
| AUTH-1 | `GET /kobanbetsu-shushi` 等の保護API（bs-003〜009参考） | 未認証（Cookieなし） | 401（想定）。フロントエンドは3秒後に自動ログアウトしログイン画面へ誘導 |
| AUTH-2 | `PUT /upload-shakatsudata`／`upload-tankadata`／`upload-sofukaridata`（bs-007〜009参考） | 一般ユーザー権限（管理者権限なし） | 403（想定。R007非機能要件「実際の認可判定はAPI側で行う」に基づく）。フロントエンドは画面自体をルートガードで到達不可としているが、URL直叩き等によりAPIへ到達した場合はAPI側403が最終防衛線となる |

## コンポーネントテスト観点

> Reacter（frontend）のコンポーネント・カスタムフックのテスト観点。`.claude/rules/reacter-testing-vitest.md` の3観点（表示要素検証・Props/State変化検証・ユーザ操作検証）＋入力バリデーションに対応する。

### 表示要素検証

| # | 対象コンポーネント | 条件 | 期待結果 |
|---|------------------|------|--------|
| CMP-表示-1 | Top | 初期表示 | お知らせ一覧（日付降順）・お問い合わせ先テーブル・「メニュー画面へ」ボタン・「メールはこちら」リンクが表示される |
| CMP-表示-2 | Menu | `userInfo.role` が `USER_ROLE.ADMIN` 以外 | 「収支管理データ参照」「メンテ機能」グループのボタンは表示されるが、「管理者用」ボタンは表示されない |
| CMP-表示-3 | Menu | `userInfo.role` が `USER_ROLE.ADMIN` | 「管理者用」ボタンが表示される |
| CMP-表示-4 | CostRiyoritsu（AG Grid セル） | セル値が100超／100／100未満／-1（データなし） | それぞれ `overCell`／`justCell`／`underCell`／`noDataCell` に対応する背景色スタイルが適用される |
| CMP-表示-5 | KobanbetsuShushi | 検索実行前（初期表示時） | 収支一覧・合計表グリッドは空、登録日時テーブルは初期値（`----/--/-- --:--:--`）を表示する |
| CMP-表示-6 | KobanbetsuShushi | 検索結果に「プロ管（計画値）」の登録日時が存在しない | 登録日時テーブルの該当行に「プロ管取込」へのリンクが表示される |

### Props・State変化検証

| # | 対象コンポーネント/フック | 条件（Props/入力の変化） | 期待結果（State/再レンダリング） |
|---|------------------|----------------------|------------------------------|
| CMP-状態-1 | KobanbetsuShushi | 「並び替え」ボタン押下で `sortingYojitsuFlg`／`sortingColumn`／`sortOrder` の組み合わせが変化 | 通信を発生させずに `shushiData`（グリッド表示データ）の並び順が更新される |
| CMP-状態-2 | useLoading | `runWithLoading` に渡した関数が実行中 | `isLoading` が `true`→（実行完了後）`false` に変化し、`LoadingLayout` の表示/非表示に反映される |
| CMP-状態-3 | AuthUserContext（AuthUserProvider） | `login({ userId, role })` 呼び出し | `userInfo` Stateが更新され、`localStorage.getItem('auth')` にも同内容がJSON文字列として保存される |
| CMP-状態-4 | ToastContext（ToastProvider） | `showToast` 連続呼び出し（表示中に別のトーストを呼び出す） | 既存のトースト・タイマーがクリアされ、新しいトーストが表示される（多重表示にならない） |
| CMP-状態-5 | AdminTorikomi | 「社員活動情報データ」フォームの送信が成功 | 「社員活動情報データ」フォームのみリセットされ、「単価データ」「ソフ仮データ」フォームの入力状態は変化しない |

### ユーザ操作検証

| # | 対象コンポーネント | 操作 | 期待結果 |
|---|------------------|-----|--------|
| CMP-操作-1 | Top | 「メニュー画面へ」ボタン押下（ログイン済み） | メニュー画面（`ROUTER.MENU`）へ遷移する |
| CMP-操作-2 | Top | 「メールはこちら」リンク押下 | 別タブでメールクライアントの作成画面（`mailto:`リンク）が起動し、Topページはそのまま表示され続ける |
| CMP-操作-3 | Login | 正しいユーザーID・パスワードを入力して送信 | ログインAPI呼び出し成功後、`login()`でコンテキスト更新し、遷移元パス（`state.from`）またはメニュー画面へ`navigate`する |
| CMP-操作-4 | Login | 誤ったユーザーID・パスワードを入力して送信 | `axiosErrorHandling`によりエラートーストが表示され、ログイン画面に留まる |
| CMP-操作-5 | Login | 保護画面（例: メニュー画面）への未ログインアクセス後にログイン成功 | `locationState.from`（例: `/menu`）へ`replace: true`で遷移する |
| CMP-操作-6 | Menu | 管理者権限で「管理者用」ボタン押下 | 管理者用データ取り込み画面（`ROUTER.ADMIN_TORIKOMI`）へ遷移する |
| CMP-操作-7 | KobanbetsuShushi | 正しい工番コード・年度を入力して「収支検索」ボタン押下 | 検索APIが呼び出され、成功時に工番名・収支一覧・合計表・登録日時テーブルが更新表示される |
| CMP-操作-8 | KobanbetsuShushi | ソート条件を選択して「並び替え」ボタン押下（検索結果が0件） | 「対象データが存在しません」等のエラーメッセージが表示され、並び替えは実行されない |
| CMP-操作-9 | KobanSearchDialog | 検索結果一覧の行をダブルクリック（または「選択」セルをクリック） | 親画面の工番コード親番・子番に選択値が反映され、収支データが自動検索され、ダイアログが閉じる |
| CMP-操作-10 | CostRiyoritsu | 検索条件を入力して「検索」ボタン押下 | コスト利用率一覧・登録日時テーブルが更新表示される |
| CMP-操作-11 | CostRiyoritsu | 一覧行（工番コードが存在する行）をダブルクリック | 工番コード・年度をStateとして引き渡し、工番別収支データ参照画面へ遷移する |
| CMP-操作-12 | Torikomi | 「フォルダ選択」でCSVを含むフォルダを選択し「送信」ボタン押下 | アップロードAPI呼び出し成功後、登録件数を含むトーストが表示され、選択状態（ファイル名表示）がクリアされる |
| CMP-操作-13 | Torikomi | 「CSV変換マクロをダウンロード」ボタン押下 | マクロファイル（`.xlsm`）のダウンロードが開始される |
| CMP-操作-14 | AdminTorikomi | 「単価データ」フォームでCSVを選択し「送信」ボタン押下 | アップロードAPI呼び出し成功後、登録件数を含むトーストが表示され、当該フォームのファイル選択状態がクリアされる |

### 入力バリデーション（Zodスキーマ）

| # | 対象フォーム | 入力 | 期待結果（エラーメッセージ） |
|---|------------|-----|---------------------------|
| CMP-検証-1 | Login（`loginSchema`） | ユーザーID未入力 | 「ユーザーIDを入力してください」相当（`MESSAGE_CODE.ERROR.COMMON.NOT_EMPTY`） |
| CMP-検証-2 | Login（`loginSchema`） | ユーザーIDが9桁以上 | 「ユーザーIDは8文字以内で入力してください」相当（`MAX_SIZE`） |
| CMP-検証-3 | Login（`loginSchema`） | パスワード未入力 | 「パスワードを入力してください」相当（`NOT_EMPTY`） |
| CMP-検証-4 | KobanbetsuShushi（`kobanbetsuShushiSchema`） | 工番コード親番が5桁 | 「工番コードの親番は6文字で入力してください」相当（`FIX_SIZE`） |
| CMP-検証-5 | KobanbetsuShushi（`kobanbetsuShushiSchema`） | 工番コード親番に英字を含む | 「工番コードの親番は半角数字で入力してください」相当（`HALF_NUMERIC`） |
| CMP-検証-6 | KobanbetsuShushi（`kobanbetsuShushiSchema`） | 年度未選択 | 「年度を選択してください」相当（`REQUIRED_SELECT`） |
| CMP-検証-7 | KobanSearchDialog（`kobanSearchDialogSchema`） | 工番コード親番・子番・工番名すべて未入力で送信 | `relatedChecks`により「検索条件を入力してください」相当のトーストが表示される（項目関連チェック） |
| CMP-検証-8 | CostRiyoritsu（`costRiyoritsuSchema`） | 工番名が16桁以上 | 「工番名は15文字以内で入力してください」相当（`MAX_SIZE`） |
| CMP-検証-9 | Torikomi（`prokanDataTorikomiSchema`） | ファイル未選択で送信 | 「ファイルを選択してください」相当（`REQUIRED_SELECT`） |
| CMP-検証-10 | Torikomi（`prokanDataTorikomiSchema`） | 選択した複数ファイルの一部が0バイト（空ファイル） | 「空のファイルは選択できません」相当（`NOT_EMPTY_FILE`） |
| CMP-検証-11 | Torikomi（`prokanDataTorikomiSchema`） | 選択したファイルにCSV形式（`text/csv`）が1件も含まれない | 「CSV形式のファイルを選択してください」相当（`INVALID_FILE_TYPE`） |
| CMP-検証-12 | AdminTorikomi（`adminShakatsuDataTorikomiSchema`） | 年月未選択 | 「年月を選択してください」相当（`REQUIRED_SELECT`） |
| CMP-検証-13 | AdminTorikomi（`adminSofukariDataTorikomiSchema`） | 年度未選択 | 「年度を選択してください」相当（`REQUIRED_SELECT`） |
| CMP-検証-14 | AdminTorikomi（`adminTankaDataTorikomiSchema`） | ファイルがCSV形式でない | 「CSV形式のファイルを選択してください」相当（`INVALID_FILE_TYPE`） |

## 連結テストシナリオ

なし（frontend → us-api → bs のスタック間連携は、us-api・bs のいずれも本リポジトリで未着手のため、本UI issueでは対象外。bs 側 SS 工程着手後に別途作成する）

## E2E テストシナリオ

> `docs/requirements/要件定義_{R001〜R007}.md` の要求一覧・受け入れ条件を全て通るシナリオを作成した。API/連結テストでカバー済みという理由でのE2E省略は行っていない。

| シナリオID  | テスト観点カテゴリ | 種別          | 起点となる機能  | 対象ロール  | テストの目的・概要                          | 事前条件           | ユーザー操作手順 (UI操作の連続) | 期待される結果 (UI表示 / DB状態) |
| ------- | --------- | ----------- | -------- | ------ | ---------------------------------- | -------------- | ------------------ | --------------------- |
| E2E-001 | 認証・ログイン   | 正常系         | トップページ | 一般ユーザー | 未ログイン状態でトップページからログインし、メニュー画面へ到達できること | ・有効な一般ユーザーID/パスワードが存在 | 1. トップページを開く（`/top`）<br>2. 「メニュー画面へ」ボタンを押下（未ログインのためログイン画面へ遷移）<br>3. ユーザーID・パスワードを入力し「ログイン」ボタンを押下 | [UI] メニュー画面が表示され、「管理者用」ボタンは非表示<br>[DB] なし（参照系のみ） |
| E2E-002 | 認証ガード     | セキュリティ      | メニュー画面／工番別収支データ参照画面／管理者用データ取り込み画面 | 未ログイン | 未ログイン状態で保護画面のURLに直接アクセスした場合にログイン画面へリダイレクトされ、ログイン後に元の画面へ戻れること | ・ログアウト状態（ローカルストレージに`auth`キーが存在しない） | 1. ブラウザで `/kobanbetsu-shushi` へ直接アクセス<br>2. ログイン画面へリダイレクトされることを確認<br>3. 正しい認証情報でログイン | [UI] ログイン画面へリダイレクトされ、ログイン成功後に工番別収支データ参照画面（`/kobanbetsu-shushi`）へ遷移する<br>[DB] なし |
| E2E-003 | 状態遷移      | 異常系（権限制御）         | メニュー画面／管理者用データ取り込み画面 | 一般ユーザー／管理者 | 権限による画面到達可否と、管理者権限でのメニューボタン表示・遷移を確認する | ・一般ユーザーと管理者ユーザーの双方が存在しログイン可能 | 1. 一般ユーザーでログインしメニュー画面を表示（「管理者用」ボタン非表示を確認）<br>2. 一般ユーザーのまま `/admin-torikomi` へ直接アクセス<br>3. ログアウトし管理者ユーザーでログイン<br>4. メニュー画面で「管理者用」ボタンを押下 | [UI] 手順2ではメニュー画面へリダイレクトされる。手順4では管理者用データ取り込み画面へ遷移する<br>[DB] なし |
| E2E-004 | 認証・ログイン   | 異常系         | ログイン画面 | 未ログイン | 誤った認証情報でのログイン失敗時にエラーメッセージが表示され画面に留まること | ・入力するユーザーID/パスワードの組み合わせが登録データと一致しない | 1. ログイン画面で誤ったパスワードを入力し「ログイン」ボタンを押下 | [UI] エラートーストが表示され、ログイン画面に留まる（画面遷移しない）<br>[DB] なし |
| E2E-005 | マスタCRUD   | 正常系（リレーション） | 工番別収支データ参照画面／工番検索ダイアログ | 一般ユーザー | 工番が不明な場合に工番検索ダイアログから工番を選択し、そのまま収支データが検索されること | ・ログイン済み<br>・検索対象の工番データが存在 | 1. 工番別収支データ参照画面で「工番検索」ボタンを押下<br>2. ダイアログで工番名の一部を入力して「検索」ボタンを押下<br>3. 検索結果一覧の行をダブルクリック | [UI] ダイアログが閉じ、工番コード欄に選択値が反映されたうえで収支一覧・合計表が自動表示される<br>[DB] なし（参照系のみ） |
| E2E-006 | 画面表示・ナビゲーション | 正常系         | コスト利用率参照画面／工番別収支データ参照画面 | 一般ユーザー | コスト利用率一覧から工番別収支データ参照画面へ、検索条件を引き継いで遷移できること | ・ログイン済み<br>・コスト利用率データが存在する工番がある | 1. コスト利用率参照画面で年度を指定し「検索」ボタンを押下<br>2. 一覧の工番コードが存在する行をダブルクリック | [UI] 工番別収支データ参照画面へ遷移し、引き渡された工番コード・年度が検索条件欄に反映されたうえで収支データが表示される<br>[DB] なし |
| E2E-007 | マスタCRUD   | 正常系         | データ取り込み画面／工番別収支データ参照画面 | 一般ユーザー | プロ管データ未登録状態からのCSV取り込みと、取り込み後の収支データ参照画面での反映確認 | ・ログイン済み<br>・「プロ管（計画値）」の登録日時が未登録（リンク表示状態） | 1. 工番別収支データ参照画面で「プロ管取込」リンクを押下しデータ取り込み画面へ遷移<br>2. 「フォルダ選択」でCSVを含むフォルダを選択<br>3. 「送信」ボタンを押下<br>4. 「メニューへ」ボタンで戻り、再度工番別収支データ参照画面を開き検索を実行 | [UI] 手順3で登録件数を含むトーストが表示される。手順4で「プロ管（計画値）」欄が登録日時表示に変わる（bs側での永続化が前提。bs未着手のため本シナリオはfrontend単体では検証できず、結合テスト工程で検証する） |
| E2E-008 | 状態遷移      | 異常系         | データ取り込み画面 | 一般ユーザー | 単体項目チェックでエラーとなる入力（未選択・空ファイル混在・CSV形式なし）で送信されないこと | ・ログイン済み | 1. ファイルを1件も選択せず「送信」ボタンを押下<br>2. 空ファイルを含むフォルダを選択し「送信」ボタンを押下<br>3. CSV以外の形式のみを含むフォルダを選択し「送信」ボタンを押下 | [UI] いずれの手順でもエラーメッセージが表示され、APIは呼び出されない（送信されない）<br>[DB] 変化なし |
| E2E-009 | 画面表示・ナビゲーション | 異常系         | 存在しないURL | 未ログイン／一般ユーザー | 未実装パスへのアクセス時に404表示されること | ・特になし | 1. 存在しないパス（例: `/not-exist-page`）へ直接アクセス | [UI] `NotFoundPage`（404相当の表示）が表示される<br>[DB] なし |
| E2E-010 | 認証ガード     | セキュリティ（セッション） | 任意の保護画面 | 一般ユーザー | 一定時間操作がない場合にセッションタイムアウトが発生し、再ログインが必要になること | ・ログイン済み・保護画面を表示中 | 1. 保護画面を表示したまま一定時間（`COMMON_CONFIG.TIMEOUT_DURATION`）操作を行わない<br>2. 警告トースト表示後もさらに操作を行わない<br>3. タイムアウトダイアログの表示を確認し画面を閉じる（再ログインを促す） | [UI] 警告トースト表示→タイムアウトダイアログ表示→ログアウトのうえログイン画面へ遷移する<br>[DB] なし |

## テストデータ

### 有効値例
```json
{
  "valid": {
    "login": { "userId": "U0001", "password": "P@ssw0rd" },
    "kobanbetsuShushi": { "kobanCdOya": "100001", "kobanCdKo": "01", "nendo": "2026" },
    "kobanSearchDialog": { "kobanCdOya": "1000", "kobanCdKo": "", "kobanNm": "サンプル" },
    "costRiyoritsu": { "kobanCdOya": "100001", "kobanCdKo": "01", "kobanNm": "", "nendo": "2026", "bushoCd": "1001" },
    "adminShakatsuData": { "yearMonth": "202608" },
    "adminSofukariData": { "nendo": "2026" }
  }
}
```

### 無効値例
```json
{
  "invalid": {
    "login": { "userId": "", "password": "" },
    "kobanbetsuShushi": { "kobanCdOya": "10000", "kobanCdKo": "1", "nendo": "26" },
    "costRiyoritsu": { "kobanNm": "16文字以上のダミー工番名で境界値超過を検証する", "bushoCd": "10" },
    "torikomiFile": { "type": "application/pdf", "size": 0 }
  }
}
```

## 更新履歴

| Ver | 更新日 | 更新者 | 更新内容 |
|---|---|---|---|
| 1.0 | 2026/08/20 | Claude (basic-design移行) | 初版（`reacter-react-tsx2doc-試作/_src` 実装コード・`docs/requirements/要件定義_R001〜R007.md` からの移行。受け入れ条件全21件のカバレッジを確認済み） |
