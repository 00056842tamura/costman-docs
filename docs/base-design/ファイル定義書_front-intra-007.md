# ファイル定義書

| 項目 | 内容 |
|---|---|
| プロダクト名 | コスト管理システム |
| 対応機能ID | front-intra-007（管理者用 データ取り込み画面） |
| 作成日 | 2026/08/20 |
| 作成者 | Claude (basic-design移行) |
| 最終更新日 | 2026/08/20 |
| 最終更新者 | Claude (basic-design移行) |

> 工程2 UI（基本設計）の成果物。`new_docs/frontend/docs/detail-design/ファイル定義書_front-intra-007.md`（工程4 SS 詳細設計・`old_docs/04.ファイル定義書.md` からの移行）を土台とする。
> `_src/features/adminTorikomi/components/AdminTorikomi.tsx`（440行）を確認した結果、この画面には
> Excelマクロダウンロード機能（front-intra-006の「CSV変換マクロをダウンロード」ボタンに相当する処理）は**存在しない**（ダウンロード用リンク生成処理・ボタン・関連state・関連関数のいずれもコード上に見当たらない）。
> したがって出力ファイル（`F-OUT-xxx`）の定義は追加しない。

---

## F-IN-002 社員活動情報データ取込ファイル

### ファイル基本情報

| 項目 | 内容 |
|---|---|
| ファイルID | F-IN-002（便宜的な採番） |
| ファイル名（論理） | 社員活動情報データ取込ファイル |
| ファイル名（物理） | 利用者が選択 |
| ファイル形式 | CSV |
| 文字コード | 不明（サーバ側で解析するため画面側のコードから判定できない） |
| 改行コード | 不明（同上） |
| ヘッダー行 | 不明（同上） |
| 入出力区分 | 入力 |
| 格納先パス | 該当なし（フォームデータとしてAPIへ送信） |
| 保存期間 | 該当なし |

### レイアウト定義

| No. | 項目名（論理） | 項目名（ヘッダー文字列） | 型 | 桁数 | 必須 | 形式 | 備考 |
|---|---|---|---|---|---|---|---|
| 不明 | 不明 | | | | | | 列構成はサーバ側で解析するため、画面側に列定義がない |

### バリデーション仕様（画面側）

| No. | 項目名 | チェック内容 | エラー時の扱い |
|---|---|---|---|
| 1 | 社員活動情報データファイル | 未選択 | メッセージ `ERROR_REQUIRED_SELECT`。送信しない |
| 2 | 社員活動情報データファイル | 大きさが0 | メッセージ `ERROR_NOT_EMPTY_FILE`。送信しない |
| 3 | 社員活動情報データファイル | 種別が `text/csv` でない | メッセージ `ERROR_INVALID_FILE_TYPE`。送信しない |

補足:
- 管理者用データ取り込み画面（front-intra-007）の「社員活動情報データ CSV取り込み」で選択し、社活データアップロードAPI（bs-007、`PUT /upload-shakatsudata`）へ「年月」とともに送信する。
- ファイル選択欄は `text/csv` のみを選択候補として表示する。選択されたファイルのうち1件目のみを送信する。
- ファイル内の年月と送信した年月が異なる場合の判定はサーバ側で行う。

### サンプルデータ

不明（列構成がサーバ側の仕様のため提示できない）。

---

## F-IN-003 単価データ取込ファイル

### ファイル基本情報

| 項目 | 内容 |
|---|---|
| ファイルID | F-IN-003（便宜的な採番） |
| ファイル名（論理） | 単価データ取込ファイル |
| ファイル名（物理） | 利用者が選択 |
| ファイル形式 | CSV |
| 文字コード | 不明 |
| 改行コード | 不明 |
| ヘッダー行 | 不明 |
| 入出力区分 | 入力 |
| 格納先パス | 該当なし |
| 保存期間 | 該当なし |

### レイアウト定義

| No. | 項目名（論理） | 項目名（ヘッダー文字列） | 型 | 桁数 | 必須 | 形式 | 備考 |
|---|---|---|---|---|---|---|---|
| 不明 | 不明 | | | | | | 列構成はサーバ側で解析するため画面側に列定義がない |

### バリデーション仕様（画面側）

| No. | 項目名 | チェック内容 | エラー時の扱い |
|---|---|---|---|
| 1 | 単価データファイル | 未選択 | メッセージ `ERROR_REQUIRED_SELECT`。送信しない |
| 2 | 単価データファイル | 大きさが0 | メッセージ `ERROR_NOT_EMPTY_FILE`。送信しない |
| 3 | 単価データファイル | 種別が `text/csv` でない | メッセージ `ERROR_INVALID_FILE_TYPE`。送信しない |

補足:
- 管理者用データ取り込み画面（front-intra-007）の「単価データ CSV取り込み」で選択し、単価データアップロードAPI（bs-009、`PUT /upload-tankadata`）へ送信する。選択されたファイルのうち1件目のみを送信する。

### サンプルデータ

不明。

---

## F-IN-004 ソフ仮データ取込ファイル

### ファイル基本情報

| 項目 | 内容 |
|---|---|
| ファイルID | F-IN-004（便宜的な採番） |
| ファイル名（論理） | ソフ仮データ取込ファイル |
| ファイル名（物理） | 利用者が選択 |
| ファイル形式 | CSV |
| 文字コード | 不明 |
| 改行コード | 不明 |
| ヘッダー行 | 不明 |
| 入出力区分 | 入力 |
| 格納先パス | 該当なし |
| 保存期間 | 該当なし |

### レイアウト定義

| No. | 項目名（論理） | 項目名（ヘッダー文字列） | 型 | 桁数 | 必須 | 形式 | 備考 |
|---|---|---|---|---|---|---|---|
| 不明 | 不明 | | | | | | 列構成はサーバ側で解析するため画面側に列定義がない |

### バリデーション仕様（画面側）

| No. | 項目名 | チェック内容 | エラー時の扱い |
|---|---|---|---|
| 1 | ソフ仮データファイル | 未選択 | メッセージ `ERROR_REQUIRED_SELECT`。送信しない |
| 2 | ソフ仮データファイル | 大きさが0 | メッセージ `ERROR_NOT_EMPTY_FILE`。送信しない |
| 3 | ソフ仮データファイル | 種別が `text/csv` でない | メッセージ `ERROR_INVALID_FILE_TYPE`。送信しない |

補足:
- 管理者用データ取り込み画面（front-intra-007）の「ソフ仮データ CSV取り込み」で選択し、ソフ仮データアップロードAPI（bs-008、`PUT /upload-sofukaridata`）へ「年度」とともに送信する。選択されたファイルのうち1件目のみを送信する。ファイル内の年度と送信した年度が異なる場合の判定はサーバ側で行う。

### サンプルデータ

不明。

---

## 更新履歴

| Ver | 更新日 | 更新者 | 更新内容 |
|---|---|---|---|
| 1.0 | 2026/08/20 | Claude (basic-design移行) | 初版（`new_docs/frontend/docs/detail-design/ファイル定義書_front-intra-007.md` を土台に基本設計工程成果物として整理。コード確認によりExcelマクロダウンロード機能は存在しないため出力ファイル定義は追加なし） |
